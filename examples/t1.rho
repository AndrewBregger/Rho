#load "test.rho"
#load "test2.rho"

a: i16;
b: i32;

h := "Hello, World";

c: f32 :: 0.0;
g := 'd';

a: byte = expr;

c := 5;

foo :: (x: *f32) -> f32 {
	if x == 1
		return x - 1;
	else if x == 2
		return x - 5;
	else
    return x;
}

foo2 :: (x: f32*, y: f32) -> f32, f32 {
    return x, y;
}

foo3 :: (x: f32, y: f32) -> a: f32,b: f32 {
    a = x;
    b = y;
}