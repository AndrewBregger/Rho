module main;
import test;
import test2;

a: i16;
b: i32;

h := "Hello, World";

c: f32 :: 0.0;
g := 'd';

a: byte = expr;

c := 5;

foo :: (x: &f32) -> f32 {
    return x;
}

foo2 :: (x: f32, y: f32) -> f32, f32 {
    return x, y;
}

foo3 :: (x: f32, y: f32) -> a: f32,b: f32 {
    a = x;
    b = y;
}